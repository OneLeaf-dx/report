# 柑橘病蟲害辨識階段性進度週報（2026-09-13）

> **報告日期**：2026-09-13（報告期間：2026-09-08 至 2026-09-13，銜接 [2026-09-07 週報](20260907_weekly_report.md)）
> **評測對象**：即時辨識功能的採用門檻、`Thrips_Damage` 的標註約定、`Datasets_YOLO26_v5.7` 的兩臂訓練與交付基準切換
> **資料來源**：訓練用程式碼庫 `OneLeaf-dx/detection` 的 `docs/` 記錄文件與訓練輸出（不在本文件庫版控範圍）；本期細節另見 §7 列出的三篇報告
> **撰寫人**：Claude Code

## 1. 摘要

- **本期回答了五個問題，每一個的判準都寫在量測之前。** 即時辨識不採用、舊的標註約定未過、改約定後通過、外部影像無效、重標解除矛盾。沒有一個是看完結果才決定怎麼算的。
- **交付基準由 v11.5 換成 v5.7。** 理由不是模型變好——標註未變動的八類平均 AP50 為 0.8527 → 0.8508——而是 `Thrips_Damage` 的數字終於有解釋力。**v5.7 的 test mAP50 0.861 不可與 v11.5 的 0.810 相比**，那是框定義換掉造成的差距。
- **從 v9 延續下來的 `Thrips_Damage` valid / test 矛盾解除**：兩人標註一致性量測指出根因是「要分成幾個框」這個自由度；改為一張葉子一個框並全類重標後，差距由 −0.202 降到 +0.121（門檻 0.165）。
- **四條線都收斂了。** 模型端、訓練協定、部署端、資料端各自被預先登記的判準關閉；兩個有效的手段修的都是「數字能否被相信」，沒有一個讓模型變好。**在不新拍照片的前提下，工程手段已經用盡。**

### 核心結果總覽

| 問題 | 判準（量測前登記） | 實得 | 裁決 |
| --- | --- | --- | --- |
| 要不要做即時辨識 | 延遲 ≤ 40 ms 且 mAP50 ≥ 0.764 且逐類跌幅 ≤ 0.10 | 延遲全過、精度最高 0.709 | 不採用 |
| 薊馬葉害的框能否一致定義 | 兩人中位 IoU ≥ 0.85 | 0.746 | 未通過 |
| 改成一張葉子一個框呢 | 同上，只重測一次 | 0.899 | 通過 |
| 外部影像對潛葉蛾有沒有用 | AP50 增益 > 0.04 | +0.021 | 未通過 |
| 重標能否解除矛盾 | valid / test 差距 < 0.165 | +0.121 | 通過 |

## 2. 背景與方法

### 2.1 上週留下的決策點

2026-09-07 週報 §6.3 留下兩個決策點：30 FPS 是否真的需要、人工標註工作是否發派。2026-09-08 週會的結論是：Benchmark 可以繼續測試，但**需要一個止損標準**；指導教授進一步說明為「**達不到標準，就不採用即時辨識的功能**」。當時兩包人工標註工作已擱置兩週，仍未發派。

### 2.2 判準一律先登記

本期每一個判準都有可查的登記時間，且早於對應的量測。

#### 表 2-1：本期判準的登記與量測時間

| 判準 | 登記 | 量測 |
| --- | --- | --- |
| 即時辨識採用門檻 | 2026-09-11（程式碼庫 commit `66747bd`） | 2026-09-11，登記之後 |
| 標註一致性 0.85 | 2026-08（v11 計畫書） | 2026-09-12 |
| 改約定重測的規則 | 2026-09-12 23:44（commit `2914698`） | 重測結果檔建立於 23:51 與 23:53 |
| 外部影像增益 0.04、矛盾門檻 0.165 | 2026-09-13 資料集驗收文件（commit `fc729a9`） | 兩臂訓練完成於其後 |

表 2-1 的四組時間都能從程式碼庫的 commit 紀錄查證，判準沒有任何一次在看到結果後修改。

## 3. 部署線：不採用即時辨識

依預先登記的門檻，在測試裝置（Snapdragon 662）上**不採用即時辨識，App 只做拍照辨識**。五個候選組合的延遲全部過線、精度全部未過，根因是延遲與精度沒有重疊的解析度：fp32 要降到 224 才進得了 40 ms，精度卻在 288 以下斷崖式下滑。唯一上限過線的是 288 解析度的量化感知訓練，但精度餘裕只有 0.004，不在預算內。部署線較 2026-09-22 的期限提前結案，未動用重訓額度。

完整量測數據依章節職責界線放在〈[YOLO26-nano-P2 即時辨識採用門檻實機測試報告](../%E7%B3%BB%E7%B5%B1%E6%B8%AC%E8%A9%A6%E8%88%87%E8%A9%95%E4%BC%B0/yolo26n_p2_realtime_gate_benchmark.md)〉，本週報只引用結論。

仍待決定的是**拍照模式要用哪一個組合**，需要團隊先訂出「按下快門後多久內要出結果」的上限。

## 4. 資料線：標註約定與 v5.7

### 4.1 工作包 B：從擱置到回收

外部潛葉蛾影像的補標原本需要組員安裝軟體、熟記規則後從零畫框，擱置兩週未發派。本期改為「**AI 依書面規則預標、使用者單人逐張審圖**」，兩天內完成：150 張、185 個框，其中 26 張無食痕、作為負樣本。審圖時 103 張有修改、刪除 13 個預標框、新增 0 框。回收過程中另發現並修正兩個會靜默破壞標註的問題：影像帶有 EXIF 旋轉標記，以及標註軟體 LabelImg 會覆寫類別清單檔。

### 4.2 工作包 A：標註約定的兩輪量測

兩位標註者各自標同樣 20 張、不互看、不預標。**第一輪依舊約定，中位 IoU 0.746，未達 0.85。** 依既定規則此時應刪除 `Thrips_Damage` 類別，但該規則所引用的前期分析對此子域的結論是「不要刪」：它是唯一符合田間部署域的薊馬訊號，而且刪除不會讓數字變好。因此改走第三條路——**只改一條約定、只重測一次、判準不變、未過即刪除**，並在重測前登記。

診斷顯示分歧集中在「要分成幾個框」而非邊界：兩人框數相同的 15 張中位已有 0.872；把每張的框合併成外包絡後的反事實預測為 0.886。**第二輪改為一張葉子一個框，實測 0.899，逐張差異的中位數為 +0.000**——機制假說被驗證。

### 4.3 v5.7 建置

`Thrips_Damage` 208 張依新約定全類重標（框 245 → 215），建成 `Datasets_YOLO26_v5.7`；另建外部影像實驗臂，124 張外部影像佔用潛葉蛾既有的增強配額，兩臂總張數相同。兩臂各 92 項驗收檢查全數通過。建置時另抓到一個靜默失敗：讀圖函式在中文路徑下回傳空值而不報錯，導致整批增強影像產出 0 張，而既有檢查因只驗比例、不驗絕對張數而照樣通過；已改用與路徑無關的讀圖方式，並加上產出 0 張即中止的保險。

資料集細節見〈[Datasets_YOLO26_v5.7 資料集統計與標註一致性報告](../%E8%B3%87%E6%96%99%E9%9B%86%E5%88%86%E6%9E%90/yolo26_v5-7_dataset_stats.md)〉。

## 5. 模型交付：換成 v5.7

### 表 5-1：交付基準切換前後

| 項目 | v11.5（前一個基準） | v5.7 對照臂（交付） | v5.7 外部影像臂 |
| --- | ---: | ---: | ---: |
| test mAP50（九類） | 0.80959 | 0.86118 | 0.86499 |
| **八類平均 AP50（可跨版本比較）** | **0.85268** | **0.85082** | **0.85884** |
| `Thrips_Damage` valid − test | −0.2017 | +0.1210 | +0.1460 |
| `Citrus_Leaf_Miner` pooled AP50 | 0.6961 | 0.7403 | 0.7612 |
| 九類 pooled ±2SE 最差值 | 0.099 | 0.099 | 0.099 |

表 5-1 中只有「八類平均」一列可以跨版本比較，它在三者之間的差距都落在雜訊範圍內；九類 mAP50 的上升來自 `Thrips_Damage` 框定義的改變。

**外部影像臂不交付**：潛葉蛾 +0.021 未達 0.04，而同一輪訓練資料完全沒動的油斑病也移動了 +0.021。訓練細節與逐類數據見〈[柑橘病蟲害 YOLO26-nano-P2 v5.7 訓練評估與交付報告](20260913_yolo26n_p2_training_report.md)〉。

引用 v5.7 的數字時，建議同時說明：

> v5.7 在新的標註約定下，九類 test mAP50 為 0.861；此數字與 v5.6 以前不可比，因為 `Thrips_Damage` 換了框定義。在標註未變動的八類上，v5.7 與 v11.5 的表現相同。

## 6. 全案盤點

### 6.1 四條線的現況

#### 表 6-1：四條線的收斂狀態

| 線 | 結論 | 依據 |
| --- | --- | --- |
| 模型端 | 連續四輪、十一種改動全部無效 | v9 六臂消融零臂通過 2 個標準差門檻；v12s 放大 3.84 倍參數只換到 +0.003 |
| 訓練協定 | 有效，但修的是可報告的評估精度 | 固定輪數並報 `last.pt`，使 valid 可與 test 併計，逐類 ±2SE 最差值由 0.140 降到 0.099 |
| 部署端 | 不採用即時辨識 | 延遲與精度沒有重疊的解析度 |
| 資料端 | 外部影像無效；重標有效但未讓模型變好 | 外部影像 +0.021；重標後八類平均 −0.002 |

表 6-1 的四條線都已被預先登記的判準關閉，兩個有效的手段都是讓數字可信，而不是讓模型變好。

### 6.2 已否決的方向

#### 表 6-2：已否決的方向與依據

| 方向 | 試過的內容 | 依據 |
| --- | --- | --- |
| 自訂架構與損失 | Wise-Inner-MPDIoU（兩種比例）、ADown、StarTripletBlock、輸入解析度 800 | 五臂全部未過 2 個標準差門檻，其中三臂低於 −2σ |
| 模型容量 | nano 換 small（3.84 倍參數），連帶否決 medium | +0.003，遠低於預先登記的 0.03 |
| 匯出參數 | 22 組；GPU delegate、NNAPI、`w8a16`、靜態 INT8、`w8a32` | 有效槓桿只有輸入解析度 |
| 即時辨識 | 224 至 288 解析度的五個組合 | 精度全部未過 |
| 影像合成與生成 | 剪貼合成、圖片生成 | 合成品質不足以通過視覺驗收，生成影像的框位置無法驗證 |
| 外部影像 | 124 張外部潛葉蛾影像 | +0.021，與未加資料的類別移動幅度相同 |
| 自動標註 | 色彩門檻找框、以現有模型預標外部影像 | 與人工的中位 IoU 僅 0.42；模型只認出 5% |
| 刪除 `Thrips_Damage` | 依中止條件本應刪除 | 刪除不會讓數字變好，且失去唯一的田間薊馬訊號；改以新約定重測後通過 |

表 6-2 的每一項都附有當時的判準與實測數字，有人提議其中任何一個方向時，應先確認新提議與既有依據的差別。

### 6.3 還開著的問題

#### 表 6-3：還開著的問題

| 問題 | 卡在哪 | 需要什麼 |
| --- | --- | --- |
| `Scale_Insect` 的框一致性 | 最弱的一類（pooled 0.632），valid / test 差距在同一份資料上於 0.040 至 0.174 之間跳動，正是框不一致的徵狀，但從未量測 | 兩人各標 20 張、中位 IoU ≥ 0.85，兩人各約半小時 |
| 拍照模式用哪一個組合 | `fp32@320`、`fp32@640`、`w8a32@640` 三選一 | 團隊訂出拍照模式可接受的延遲上限 |
| 結論只成立於一台裝置 | 目前只量過 Snapdragon 662 | 另有兩台高階裝置可加測，是否進行尚未決定；若加測須另行登記判準並納入持續負載 |
| v5.7 權重的行動端匯出 | 部署端的 TFLite 檔仍由 v11.5 權重匯出 | 選定拍照模式的組合後，只需以 v5.7 權重重新匯出並重評該組合 |
| 模型精度 | 不新拍照片的前提下已無路可走 | 補拍同域照片（v11 計畫書估算 B 級目標需 353 張） |

表 6-3 的前兩項成本最低，第五項是模型精度唯一還沒走過的路。

### 6.4 下一步

若專題時間允許，依投報率排序：

1. **量 `Scale_Insect` 的框一致性**——成本最低，且有明確徵狀。
2. **由團隊決定拍照模式的延遲上限**，選定行動端組合，並以 v5.7 權重重新匯出。
3. **決定是否補拍照片**——這是模型精度唯一還沒走過的路。

## 7. 結論與限制

1. **即時辨識不採用，App 只做拍照辨識。**
2. **`Thrips_Damage` 保留**，並依一張葉子一個框的約定全類重標；valid / test 矛盾解除。
3. **交付基準換成 v5.7 對照臂**；外部影像臂不交付。
4. **四條線收斂**，在不新拍照片的前提下工程手段已用盡。

本期產出的三篇報告：

- 〈[Datasets_YOLO26_v5.7 資料集統計與標註一致性報告](../%E8%B3%87%E6%96%99%E9%9B%86%E5%88%86%E6%9E%90/yolo26_v5-7_dataset_stats.md)〉
- 〈[柑橘病蟲害 YOLO26-nano-P2 v5.7 訓練評估與交付報告](20260913_yolo26n_p2_training_report.md)〉
- 〈[YOLO26-nano-P2 即時辨識採用門檻實機測試報告](../%E7%B3%BB%E7%B5%B1%E6%B8%AC%E8%A9%A6%E8%88%87%E8%A9%95%E4%BC%B0/yolo26n_p2_realtime_gate_benchmark.md)〉

本報告未涵蓋：

- **實機延遲的完整數據**：依章節職責界線見〈[系統測試與評估](../README.md#系統測試與評估)〉
- **資料集統計與逐張一致性數據**：見〈[資料集分析](../README.md#資料集分析)〉
- **人工標註工作包的操作流程細節與建置工具的程式實作**：屬工程紀錄，保留在訓練用程式碼庫
- **第二台裝置的量測**：尚未決定是否進行
